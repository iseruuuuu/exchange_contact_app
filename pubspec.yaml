name: exchange_contact_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+2

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  freezed:
  flutter_state_notifier:
  state_notifier:
  build_runner:

 #SharePreference
  native_shared_preferences:
  shared_preferences:
  enum_to_string:

  #QR
  ai_barcode: ^3.0.1
  qr_flutter:

  #tutorial
  flutter_overboard: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
